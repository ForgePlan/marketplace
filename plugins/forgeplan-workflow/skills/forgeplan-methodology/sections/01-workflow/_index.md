# Workflow

The forgeplan workflow follows a structured cycle: **Route -> Shape -> Build -> Audit -> Evidence -> Activate**.

Each stage has a clear purpose and produces specific outputs. The depth of each stage scales with the complexity of the task.

## Sections

- [route-shape-build.md](route-shape-build.md) — Full cycle explanation with commands and decision points.
