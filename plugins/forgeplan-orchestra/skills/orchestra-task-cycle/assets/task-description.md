## Why

What this unblocks, or what breaks without it. Two or three sentences. State the consequence of not
doing it, not a restatement of the title.

If a code-truth audit produced a relevant finding, cite it here with `file:line` — that is what
stops the next reader from re-deriving it.

## Done when

- <observable a sceptic could check>
- <observable a sceptic could check>
- <observable a sceptic could check>

Each line names something openable: a command that exits zero, a status that reads a value, a test
that fails without the change. "Works correctly" is not an observable.

## Notes

| | |
|---|---|
| Artifact | `RFC-001` — depth, R_eff if known |
| Evidence | `EVID-001` — verdict |
| Blocked by | task IDs, or "— nothing, ready to pick up" |
| Unblocks | task IDs that become startable |
| Roadmap | `docs/ROADMAP-*.md` |

Anything a reader needs that the artifact does not carry. Warnings about wrong paths, inherited
claims that are unverified, constraints from a prior decision.
