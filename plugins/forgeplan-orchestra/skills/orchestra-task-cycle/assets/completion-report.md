## Что сделано

Что изменилось на самом деле. Файлы, коммиты, артефакты — со ссылками. Не пересказ описания задачи.

## Что не сделано

Пункты гейта, оставшиеся неотмеченными, и **почему**. Область, которую сознательно не трогали.
Раздел, который остался пустым.

Отчёт без этого раздела — не отчёт. Если действительно сделано всё, так и написать одной строкой —
но написать.

## На что смотреть

Что может сломаться из-за этого изменения. Допущения, которые не проверялись. Что должен знать
следующий, кто сюда придёт.

## Как откатить

Если изменение нетривиально — команда или последовательность. Если тривиально — опустить раздел.

---

Заметки по формату:

- Markdown рендерится. Заголовки, списки, таблицы, ссылки, блоки кода — всё работает.
- Упоминания сущностей: `<!task@uid[Название]>`, `<!document@uid[Название]>`,
  `<!member@uid[Имя]>`. Типы: task, project, member, channel, group, document.
- Чаты задач разрешены без спроса. Каналы, группы и личные сообщения — **спрашивать**.
- Голое `09:00` в тексте превращается в тайм-чип со сдвигом часового пояса. Экранировать или
  переформулировать.
