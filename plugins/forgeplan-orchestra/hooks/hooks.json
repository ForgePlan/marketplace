{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check the command. ONLY respond if it contains 'forgeplan activate' or 'forgeplan new'. For those: say 'Consider /sync to keep Orchestra in sync.' For ALL other commands: respond with EXACTLY an empty string. No explanation.",
            "timeout": 10
          }
        ]
      }
    ]
  }
}