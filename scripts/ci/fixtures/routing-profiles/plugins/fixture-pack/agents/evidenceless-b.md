---
name: evidenceless-b
description: |
  EN: Fixture — labelled B, but its allowlist contains no forgeplan tool, so it cannot create the
      EVIDENCE its routing row demands. This is the marketplace#236 / EVID-231 defect, frozen.
  RU: Фикстура — помечен B, но в аллоулисте нет ни одного forgeplan-инструмента, поэтому создать
      EVIDENCE, которого требует его строка роутинга, он физически не может.
  Triggers: "fixture"
model: sonnet
color: "#123456"
tools: Read, Bash, Glob, Grep
---
Fixture body.
