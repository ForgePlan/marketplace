# Fixture agent with no frontmatter at all

An agent whose frontmatter fails to parse loads with empty metadata, so every constraint it
declares silently does not apply — the marketplace#244 failure class.
